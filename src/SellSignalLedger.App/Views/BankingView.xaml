<UserControl x:Class="SellSignalLedger.App.Views.BankingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Banking" Style="{StaticResource HeadingText}"/>

        <Border Grid.Row="1" Background="{StaticResource PaperRaisedBrush}" CornerRadius="6" Padding="16" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <StackPanel Width="220" Margin="0,0,16,0">
                    <Label Content="CASH BALANCE"/>
                    <TextBlock x:Name="BalanceText" FontSize="24" FontWeight="SemiBold"/>
                </StackPanel>
                <StackPanel Width="160" Margin="0,0,16,0">
                    <Label Content="AMOUNT"/>
                    <TextBox x:Name="AmountBox" Margin="0"/>
                </StackPanel>
                <StackPanel Width="220" Margin="0,0,16,0">
                    <Label Content="NOTE (OPTIONAL)"/>
                    <TextBox x:Name="NoteBox" Margin="0"/>
                </StackPanel>
                <StackPanel VerticalAlignment="Bottom" Orientation="Horizontal">
                    <Button Content="Deposit" Background="{StaticResource BlueBrush}" Foreground="White" Click="Deposit_Click"/>
                    <Button Content="Withdraw" Background="{StaticResource BlueBrush}" Foreground="White" Click="Withdraw_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" x:Name="Grid" ItemsSource="{Binding}" CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="WHEN" Binding="{Binding Timestamp}" SortMemberPath="TimestampSort" Width="150"/>
                <DataGridTextColumn Header="TYPE" Binding="{Binding Type}" Width="100"/>
                <DataGridTemplateColumn Header="AMOUNT" SortMemberPath="AmountSort" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Amount}" Foreground="{Binding AmountBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="BALANCE AFTER" Binding="{Binding BalanceAfter}" SortMemberPath="BalanceAfterSort" Width="130"/>
                <DataGridTextColumn Header="NOTE" Binding="{Binding Note}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
